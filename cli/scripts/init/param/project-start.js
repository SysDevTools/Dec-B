module.exports = (commandLine) => {
    commandLine += ' --project-start';
    console.log("Criando e publicando as branches main e develop...");
    // Adicione aqui o código necessário para realizar essas operações.
    return commandLine;
};
