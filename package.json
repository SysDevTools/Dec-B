{
  "name": "dec-b",
  "private": true,
  "workspaces": [
    "cli"
  ],
  "scripts": {
    "start": "npm start --workspace cli",
    "test": "npm test --workspace cli",
    "build": "npm run build --workspace cli"
  }
}
